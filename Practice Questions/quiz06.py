# Create a script that imports the `sys` module and prints the Python interpreter's executable path and version info.

import sys


print("Python Executable path", sys.executable)
print("Python version:", sys.version)
